---
title : Django Modal 양식 문서 (번역)
last_modified_at: 2019-01-15T10:45:06-05:00
header:
  overlay_image: /assets/images/code/django_pro.png
categories:
  - django
tags: 
    - modal
    - django
toc: true 
---


# 모달 장고 양식 문서 
- 마리오 올란디 <small>2018년 9월 14일</small>

**[GitHub](https://github.com/morlandi/editing-django-models-in-the-frontend)**

**[이탈리아어 원문 pdf](https://buildmedia.readthedocs.org/media/pdf/modal-django-forms/latest/modal-django-forms.pdf)**

프론트 엔드에서 편집 기능을 추가하려면 모달 양식을 사용하면 유용 합니다.

이번 프로젝트는 **modal Popup, 양식 제출** 및 **Ajax를 활용한 유효성 검사** 로써 장고에서 구현하는 기술 예제 모음입니다. 효과적인 방법으로 반복을 최소화 하는 코드를 구성하도록 합니다.

<br/>
# Modal Basic

## **JavaScript 로 구현하는 Modal**

**HTML5 에서는 기능을 제공하지 않습니다.** 일부 필요에 따라 문서의 **일부분을 숨기거나 활성화 하는 방법** 으로 구현을 합니다. <small><strike>당신 바람피우는거야? 농담도 이탈리아식이군..</strike></small>

<figure class="align-center">
  <img src="{{site.baseurl}}/assets/images/code/modal-js.jpg">
  <figcaption>https://www.w3schools.com/howto/tryit.asp?filename=tryhow_css_modal2</figcaption>
</figure>

처음에는 숨겨진 상태로 있는, 반투명한 속성의 **모달 콘텐츠 (modal content)** 는 **Pop up** 스타일로 지정됩니다. **CSS** 특성을 활용하여 Modal 을 표시하거나 숨깁니다. 다음의 예제는 **JQuery** 를 활용하여 구현한 예시 입니다.

```javascript
$(document).ready(function() {
    // 모달 가져 오기
    var modal = $('#my-modal');
    // Open the modal
    var button = $('#button-open-modal');
      button.on('click', function(event) {
      modal.css('display', 'block');
    })
    // Close the modal
    var close_button = $('.close');
      close_button.on('click', function(event) {
      modal.css('display', 'none');
    })
    // 활성화 되지 않은 부분을 선택시 자동을 닫습니다
    $(window).on('click', function(event) {
      if (event.target.id == modal.attr('id')) {
      modal.css('display', 'none');
    }
  });
});
```

## **값을 반환하는 Modal**

Modal 에서 어떠한 값을 갖고서 메인 페이지로 돌아가는 기능도 구현할 수 있습니다. 

```javascript
function close_popup(modal) {
  var value = modal.find('.my-modal-body input').val();
    save_text_value(value);
    modal.hide();
}
function save_text_value(value) {
  if (value){
    $('#result-wrapper').show();
    $('#result').text(value);
  }else{
    $('#result-wrapper').hide();}
}
```

이때 주의할 점으로는 **modal 입력값을 전달시 입력값을 초기화 한 뒤에 전달을 합니다.** 그렇지 않으면 기존의 입력 내용을 계속 적용하게 됩니다.

```javascript
function open_popup(modal) {
  var input = modal.find('.my-modal-body input');
    input.val('');
    modal.show();
    input.focus();
}
```

## **Bootstrap 을 활용한 Modal**

**'modal-lg’** 또는 **‘modal-sm’ class** 를 활용하여 Bootstrap 의 플러그 인을 구현할 수 있습니다. **[(Doc)](https://getbootstrap.com/docs/3.3/javascript/#modals)**

<br/>
# Django 에서 Modal 구현하기

## 목적
- 특정한 **Container** 를 활용하여 FrontEnd 에서 레이아웃을 정의합니다
- 대화의 내용 및 생명주기는 **Server** 에서 관리를 합니다
- 사용자가 작업을 완료하거나 취소를 하면, **Container** 가 닫힙니다

## 비어있는 대화상자
`/frontend/templates/frontend/generic_empty_modal_for_django.html` 에 구현된 **Container** 는 **Template** 로 구현을 하고, 상태별 스타일은 **modals.css** 에서 정의를 합니다

```html
<a href="#" onclick="openModalDialog(event,'#modal_generic'); return false;">
  <i class="fa fa-keyboard-o"></i>modal 을 활성화 합니다 (네),(아니오)</a>
```

정의한 template 에 필요한 모든 요소를 ​​포함하는 경우에는 이를 **재사용** 함으로써 보다 효율적인 작업이 가능 합니다.

```html
<a href=""
  data-dialog-class="modal-lg"
  data-title="Set value"
  data-subtitle="Insert the new value to be assigned to the Register"
  data-icon="fa-keyboard-o"
  data-button-save-label="Save"
  onclick="openModalDialog(event, '#modal_generic'); return false;">
  <i class="fa fa-keyboard-o"></i>modal 을 활성화 합니다 (네),(아니오)
</a>
```

`/frontend/static/frontend/js/modals.js`  javascript 의 도움을 필요로 합니다. 내용을 활성화 하기전에 세부내용을 정의한 뒤, 활성화 실행시 메인 페이지의 템플릿에 세부 정보를 출력하는 방식으로 컨텍스트를 활용하실 수 있습니다.

```javascript
function initModalDialog(event, modal_element) {

  var modal = $(modal_element);
    if (modal.length <= 0) {
        console.log('ERROR: modal "%o" not found', modal_element);
        display_server_error(sprintf('ERROR: modal "%s" not found', modal_element));
    }
    var target   = $(event.target);
    var title    = target.data('title') || '';
    var subtitle = target.data('subtitle') || '';
    // either "modal-lg" or "modal-sm" or nothing
    var dialog_class = (target.data('dialog-class') || '') + ' modal-dialog';
    var icon_class   = (target.data('icon') || 'fa-laptop') + ' fa modal-icon';
    var button_save_label = target.data('button-save-label') || 'Save changes';

    modal.find('.modal-dialog').attr('class', dialog_class);
    modal.find('.modal-title').text(title);
    modal.find('.modal-subtitle').text(subtitle);
    modal.find('.modal-header .title-wrapper i').attr('class', icon_class);
    modal.find('.modal-footer .btn-save').text(button_save_label);
    modal.find('.modal-body').html('');

    // Annotate with target (just in case)
    modal.data('target', target);

    if (modal.hasClass('draggable')) {
        modal.find('.modal-dialog').draggable({
            handle: '.modal-header'
        });
    }
    return modal;
}

function openModalDialog(event, modal) {
    if ($.type(modal) == 'string') {
        modal = initModalDialog(event, modal);
    }
    modal.modal('show');
}
```

## Modal 에 draggable 기능 추가하기

이 기능에는 `JQuery-UI.js` 를 필요로 합니다.

```html
<div class="modal draggable" id="modal_generic" tabindex="-1" role="dialog" aria-hidden="true">
<div class="modal-dialog">
```

```javascript
if (modal.hasClass('draggable')) {
  modal.find('.modal-dialog').draggable({
  handle: '.modal-header'
  });
}
```

다음의 내용을 CSS 스타일로 추가를 하면 유용합니다

```css
.modal.draggable .modal-header {
  cursor: move;
}
```

## Django 에서의 파일의 구성

modal 의 세부 내용들을 개별 파일로 분리하여 저장, 관리를 합니다.

{% raw %}
```php
{% block modals %}
  {% include 'frontend/modals/generic.html' %}
  {% include 'frontend/modals/dialog1.html' %}
  {% include 'frontend/modals/dialog2.html' %}
{% endblock modals %}
```
{% endraw %}

## 간단한 modal 구현하기

Ajax 를 사용하면, 대화상자의 내용을 제어할 수 있습니다.

```javascript
function openMyModal(event) {
  var modal = initModalDialog(event, '#modal_generic');
  var url = $(event.target).data('action');
  modal.find('.modal-body').load(url, function() {
    modal.modal('show');
})
```

15페이지 까지 번역을 했는데, 실제로 쓰기엔 장벽이 너무많아 그만하려구요.. ㅠㅠ..

우선은 지금까지 구한 모듈을 보다 효과적으로 활용하는데 집중을 하자..