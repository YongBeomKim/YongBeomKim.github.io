---
title : Django Modal 양식 문서 (번역)
last_modified_at: 2019-01-15T10:45:06-05:00
header:
  overlay_image: /assets/images/code/django_pro.png
categories:
  - django
tags: 
    - modal
    - django
toc: true 
---


# 모달 장고 양식 문서 
- 마리오 올란디 <small>2018년 9월 14일</small>

**[GitHub](https://github.com/morlandi/editing-django-models-in-the-frontend)**

**[원문 pdf](https://buildmedia.readthedocs.org/media/pdf/modal-django-forms/latest/modal-django-forms.pdf)**

**기능별 endpoint** 를 모두 지정하는 어려움을 극복하기 위해, Ajax을 활용하여 양식 제출 및 유효성 검사, 효과적인 방법으로 코드를 구성하는 예제를 정리해 보겠습니다

<br/>
# Modal Basic

## **HTML 에서 Modal**

HTML5 기본 문법에는 Pop Up 을 제공하지 않아서, HTML 일부 조각을 양식화 한 뒤 이를 숨김 속성을 활용합니다.

## **JavaScript 로 구현하는 Modal**

<figure class="align-center">
  <img src="{{site.baseurl}}/assets/images/code/modal-js.jpg">
  <figcaption>https://www.w3schools.com/howto/tryit.asp?filename=tryhow_css_modal2</figcaption>
</figure>

반투명하고 처음에는 숨겨진 상태의 "모달"요소는 **전체 HTML 의 일부분으로** 제공됩니다. **모달 콘텐츠 (modal content)** 는 활성화시 **팝업 창으로** 보이도록 스타일이 지정되었습니다.
이를 활용하여 CSS 스타일로 모달의 재생 및 숨김이 가능합니다.

```javascript
$(document).ready(function() {
    // 모달 가져 오기
    var modal = $('#my-modal');
    // Open the modal
    var button = $('#button-open-modal');
      button.on('click', function(event) {
      modal.css('display', 'block');
    })
    // Close the modal
    var close_button = $('.close');
      close_button.on('click', function(event) {
      modal.css('display', 'none');
    })
    // 활성화 되지 않은 부분을 선택시 자동을 닫습니다
    $(window).on('click', function(event) {
      if (event.target.id == modal.attr('id')) {
      modal.css('display', 'none');
    }
  });
});
```

## **값을 반환하는 Modal**

위의 함수로써 Modal 기능을 구현하고, 결과값을 전달하는 함수를 정의합니다. 

```javascript
function close_popup(modal) {
  var value = modal.find('.my-modal-body input').val();
    save_text_value(value);
    modal.hide();
}
function save_text_value(value) {
  if (value){
    $('#result-wrapper').show();
    $('#result').text(value);
  }else{
    $('#result-wrapper').hide();}
}
```

이때 주의할 점으로는 **modal 입력값을 전달시 입력값을 초기화 한 뒤에 전달을 합니다.** 그렇지 않으면 기존의 입력 내용을 계속 적용하게 됩니다.

```javascript
function open_popup(modal) {
  var input = modal.find('.my-modal-body input');
    input.val('');
    modal.show();
    input.focus();
}
```

## **Bootstrap 을 활용한 Modal**

**'modal-lg’ 또는 ‘modal-sm’ class** 속성값을 활용하는 Bootstrap 의 플러그 인을 활용하여 modal 기능을 구현할 수 있습니다. **[(Doc)](https://getbootstrap.com/docs/3.3/javascript/#modals)**

<br/>
# Django 에서 Modal 구현하기


