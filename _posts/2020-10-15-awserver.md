---
title : AWS Server Instance
last_modified_at: 2020-10-15T10:45:06-05:00
header:
   overlay_image: /assets/images/project/amazon_ec2.jpg
categories:
  - server
tags: 
    - aws
    - server
    - linux
---

이번에 새롭게 AWS 서버를 개설 하게 되었습니다. AWS 에서의 서버 인스턴스 생성절차와 관련하여 정리해 보겠습니다. 가입절차 및 **EC2** 서버의 실행은 다음의 동영상을 따라서 진행하면 됩니다.

<iframe width="560" height="315" src="https://www.youtube.com/embed/oGQ1HteFYnQ" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>

<br/>

# **AWS INSTANCE USER SETTING**

동영상에서 진행한 내용을 요약하면 다음과 같습니다. `1.AWS 가입절차`, `2.AWS Console 로그인 및 EC2 인스턴스 생성`, `3.**사용자 pem** 키파일을 활용한 접속`, `4.탄력적 IP 발급후 인스턴스 연결`

인스턴스의 생성 및 기본실행이 완료 되었으면, 앞으로 정리할 내용을 살펴보면 다음과 같습니다.

1. **root** 계정의 활성화
2. **pem** 키파일 없이 로그인

## **Root 계정의 활성화**

다음의 내용은 **[EC2 root 계정 활성화](https://goddaehee.tistory.com/193)** 를 간단하게 요약하였습니다.

1. **pem** 로 서버에 접속 합니다.
2. `$sudo passwd root` 로 root 의 비밀번호를 설정 합니다.
3. `$sudo vi /etc/ssh/sshd_config` 에서 **PermitRootLogin yes** 변경 후 저장 합니다.
4. `$sudo mkdir /root/.ssh` 폴더를 생성 합니다.
5. `$sudo cp /home/ubuntu/.ssh/authorized_keys /root/.ssh` EC2 유저의 인증키를 root 에게 복사 합니다.
6. `$sudo systemctl restart sshd` ssh 를 재실행 합니다.
7. `$ssh -i 'C:\키페어경로\키페어.pem' root@접속IP` 로 접속을 확인 합니다.

## **pem 인증키 파일없이 별도 사용자 로그인 활성화**

EC2 유저 및 root 는 pem 키파일을 활용하는 방법을 추천합니다. 반면 **[추가 사용자](https://ithub.tistory.com/215)** 를 활용하면 별도의 인증키 없이도 접속 설정이 가능 합니다.

1. 최초에는 .pem 파일을 사용하여 EC2에 접속합니다.
2. `$ sudo useradd -s /bin/bash -m -d /home/USERNAME -g root USERNAME` 새로운 유저를 생성합니다.
3. `$ sudo passwd USERNAME` 유저 비밀번호를 설정 합니다.
4. 패스워드를 입력 합니다
5. `$ sudo chmod u+w /etc/sudoers` sudoers 파일 권한 변경 
6. sudoers 파일 열고, username 추가 합니다.
   ```r
   $ sudo vi /etc/sudoers
   USERNAME ALL=(ALL:ALL) ALL
   ```
7. sshd_config 파일에 PasswordAuthentication 설정값의 변경
   ```r
   $ sudo vi /etc/ssh/sshd_config
   PasswordAuthentication yes
   ```
8. `$ sudo service ssh restart` ssh를 재시작

<br/>

# Install Modules

EC2 에서 작업을 진행하기 위해서는, 앞서서 만든 계정들 중, **root** 계정을 활용하여 접속을 하고 진행하는게 가장 편리합니다. <strike>별도의 인증 과정없이 설치가 진행 가능하니까요</strike>

## Z shell

```r
apt-get install zsh
chsh -s `which zsh`
wget https://github.com/robbyrussell/oh-my-zsh/raw/master/tools/install.sh -O - | zsh
sh -c "$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions
```

필요한 내용을 설치한 뒤, 설정값에서 Plugin 과 Theme 를 추가 합니다.

```r
$ nano .zshrc                 # 설정파일
   export LANG="ko_KR.UTF-8" # 한글 인코딩을 해결
   ZSH_THEME="agnoster"      # 테마를 정의한다
   plugins=(
     git
     zsh-autosuggestions
     #zsh-syntax-highlighting
     history-substring-search
   )

$ souce .zshrc                # 변경된 설정을 적용
```

## Python 3.8

[Python](https://www.python.org/downloads/release/python-386/) 정식 사이트에서 설치파일의 경로를 확인 하고 설치를 진행 합니다. Python 내용을 추가하기에 앞서서 `openssl` 을 설치를 해야만 `pip` 로 설치시 **[SSLError("Can't connect to HTTPS URL because the SSL module is not available.](https://m.blog.naver.com/theswice/221813774822)** 오류를 피할 수 있습니다.

```r
# Install open ssl
apt install openssl -y
apt install libssl1.0-dev -y
apt install libssl1.0 -y
apt install apt-transport-https -y

# Install Dependancy Modules
apt-get install --reinstall build-essential zlib1g-dev dpkg-dev libffi-dev

# get Python tgz file and Installation
wget https://www.python.org/ftp/python/3.8.6/Python-3.8.6.tgz
tar zxf Python-3.8.6.tgz
cd Python-3.8.6/
./configure --enable-optimizations
make altinstall
```

## Django Setting with NGINX

앞서 설치한 Python 을 활용하는 Django 의 실행 및 Nginx 와의 연결 및 실행과 관련된 내용을 정리 합니다. 다음 페이지에서는 MySQL 과 POSTGRESQL 설치 및 설정과 관련된 내용을 살펴보도록 하겠습니다. 


# MariaDB
   22  sudo apt-get install python3-pip python3-venv
   23  sudo apt install mariadb-server
   24  mysql_secure_installation
   28  sudo vi /etc/mysql/conf.d/mysql.cnf 
   29  sudo vi /etc/mysql/mariadb.conf.d/50-server.cnf 
   30  service mysql restart
   31  systemctl restart mariadb
   32  mysql_secure_installation
   33  mysql

# POSTGRESQL
   36  sudo apt install postgresql postgresql-contrib
   37  sudo service postgresql status
   38  sudo -u postgres psql
   55  chmod u+x install.sh 
   56  source install.sh 



https://zamezzz.tistory.com/301

https://cjh5414.github.io/how-to-deploy-django-uwsgi-nginx-in-ubuntu/

4076
sudo wget https://www.python.org/ftp/python/3.8.5/Python-3.8.5.tgz


## 참고사이트

- **[YouTube Link](https://youtu.be/afgPtpw4NKo)**
- **[GitHub of VirtScreen](https://github.com/kbumsik/VirtScreen)**
- **[remote how to ubuntu](https://askubuntu.com/questions/1033436/how-to-use-ubuntu-18-04-on-vnc-without-display-attached)**
- **[xrandr missing hdmi output after install nvidia driver](https://askubuntu.com/questions/1097033/xrandr-missing-hdmi-output-after-install-nvidia-driver)**
